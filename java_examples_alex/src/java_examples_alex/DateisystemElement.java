package java_examples_alex;

/**
 * Base component in a simple file system composite that can display itself with indentation.
 */
public interface DateisystemElement {
    void anzeigen(String einzug);
}
